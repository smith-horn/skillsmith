# Skillsmith Website Robots.txt

# Default: Allow all crawlers
User-agent: *
Allow: /

# Block crawlers from private/auth/account paths to preserve crawl budget.
# These paths are disallowed to save crawl budget — they are not intended for
# indexing. /login/ is intentionally omitted from Disallow: it carries a
# noindex meta tag, but Googlebot must be able to crawl it to read and honor
# that directive. Disallowed pages cannot be read by Googlebot, so their
# noindex tags have no effect — the Disallow rule itself prevents indexing.
Disallow: /account/
Disallow: /auth/
Disallow: /verify/
Disallow: /signup/success/

# AI Crawlers - Explicitly Allowed
# These crawlers power AI search and answer engines

# OpenAI (ChatGPT, GPT-4)
User-agent: GPTBot
Allow: /

# OpenAI ChatGPT plugins
User-agent: ChatGPT-User
Allow: /

# Anthropic (Claude)
User-agent: ClaudeBot
Allow: /
User-agent: Claude-Web
Allow: /

# Google AI (Gemini, Bard)
User-agent: Google-Extended
Allow: /

# Perplexity AI
User-agent: PerplexityBot
Allow: /

# Cohere
User-agent: cohere-ai
Allow: /

# Meta AI
User-agent: FacebookBot
Allow: /

# Common Crawl (used by many AI training datasets)
User-agent: CCBot
Allow: /

# Microsoft/Bing AI (Copilot)
User-agent: bingbot
Allow: /

# Apple (Siri, Spotlight)
User-agent: Applebot
Allow: /

# You.com AI
User-agent: YouBot
Allow: /

# Sitemap
Sitemap: https://www.skillsmith.app/sitemap-index.xml

# LLMs.txt - AI-specific site information
# See: https://www.skillsmith.app/llms.txt
