# Create your own skill

Run **Skillsmith: Create Skill** from the palette. A wizard prompts for author, name, description, and type.

The command delegates to `@skillsmith/cli` — install it with `npm install -g @skillsmith/cli` if it is not on your `$PATH`.

On success, `SKILL.md` opens in the editor and the new skill appears in the Skills view.
