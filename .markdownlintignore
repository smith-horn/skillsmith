# git-crypt encrypted directories
# These files appear as binary in CI (where git-crypt is not unlocked)
# and generate thousands of false markdown lint errors
docs/**
.claude/**
!.claude/development/**
!.claude/templates/**
