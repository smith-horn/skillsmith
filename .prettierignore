dist/
node_modules/
coverage/
*.md
pnpm-lock.yaml
package-lock.json

# Auto-generated directories
.astro/

# Scripts with embedded GraphQL that Prettier cannot parse
scripts/linear-api.mjs

# Git-crypt encrypted directories - cannot be parsed when locked (CI, fresh clones)
# Must match encrypted paths in .gitattributes
docs/
# .claude/skills, .claude/plans, .claude/hive-mind are now private submodules
# (smith-horn/skillsmith-strategy, per-branch refs). Prettier walks the
# filesystem, so submodule mount-points are NOT auto-skipped despite the
# git boundary; explicit entries required (SMI-4829 Wave 5 push-time fix).
.claude/skills/
.claude/plans/
.claude/hive-mind/
supabase/functions/
supabase/migrations/

# Git worktrees - each worktree has its own encrypted content and Prettier config
.worktrees/
worktrees/
# Worktrees created outside `.worktrees/` (e.g. top-level `smi-<n>-*` dirs).
# Each worktree carries its own encrypted / unformatted content tree.
smi-*/

# Local settings (untracked, varies per developer)
.claude/settings.local.json
docker-compose.override.yml
